import {SAVE_ORDER} from '../actions/types';

export default function(state, action) {
    switch (action.type) {
        case SAVE_ORDER:
            default:
            return {
                ...state
            }
        
    }
}